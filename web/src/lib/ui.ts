// Shared button looks. Lime (primary) is for the one main action on a screen.
export const btn = {
  primary:
    "inline-flex h-14 items-center justify-center gap-2.5 rounded-full bg-primary px-7 text-[17px] font-semibold text-primary-foreground transition-colors hover:bg-primary/90",
  outline:
    "inline-flex h-11 items-center justify-center gap-2 rounded-full border border-line px-[18px] text-sm font-semibold transition-colors hover:border-primary hover:text-primary",
  icon: "inline-flex size-11 shrink-0 items-center justify-center rounded-full border border-line transition-colors hover:border-primary hover:text-primary",
};
